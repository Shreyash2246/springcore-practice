There is Two type of Injections in SpringCore 1.Setter & 2.Constructor SO i done both.
Also i defined Bean in three types in both Setter and Constructor. 
1.by using property&value tag, 2.by using self closing property tag means using attribute name directly, 3.by using P & C schemas as injection.
